import React, { useState } from 'react'
import { Helmet } from 'react-helmet'
import TaxList from '@/components/accountancy/settings/taxes/taxList'

const TaxComponent = () => {
  return (
    <div>
      <TaxList />
    </div>
  )
}

export default TaxComponent
